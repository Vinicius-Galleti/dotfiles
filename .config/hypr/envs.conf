# Variáveis de ambiente do Hyprland.
# Por padrão, sem nada NVIDIA-específico — funciona com AMD/Intel/sem GPU dedicada.
# Se você tem GPU NVIDIA Turing+ com GSP firmware, descomente o bloco abaixo:

# env = NVD_BACKEND,direct
# env = LIBVA_DRIVER_NAME,nvidia
# env = __GLX_VENDOR_LIBRARY_NAME,nvidia
